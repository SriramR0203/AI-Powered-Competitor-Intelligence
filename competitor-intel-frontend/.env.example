# ═══════════════════════════════════════════════════════════════════════════════
# AI-Powered Competitor Intelligence Platform — Frontend Environment Variables
#
# LOCAL DEVELOPMENT:
#   cp .env.example .env
#   The defaults below work out of the box with the Spring Boot dev server.
#
# VERCEL PRODUCTION:
#   Do NOT commit .env.  Instead set these in:
#   Vercel Dashboard → Project → Settings → Environment Variables
# ═══════════════════════════════════════════════════════════════════════════════

# ── Backend base URL — NO trailing slash ──────────────────────────────────────
# Local development (H2 backend):
VITE_API_BASE_URL=http://localhost:8080

# Production (Render) — set this in Vercel Dashboard → Environment Variables,
# or let .env.production handle it for local production builds:
# VITE_API_BASE_URL=https://ai-powered-competitor-intelligence.onrender.com

# ── Request timeout in milliseconds ──────────────────────────────────────────
VITE_API_TIMEOUT=15000
