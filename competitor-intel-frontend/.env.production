# Production environment — loaded automatically by Vite during `npm run build`.
# Values here override .env for production builds only.
# Do NOT commit real secrets to this file.
#
# This file IS safe to commit because it contains no secrets —
# only the public backend URL.
VITE_API_BASE_URL=https://ai-powered-competitor-intelligence.onrender.com
VITE_API_TIMEOUT=15000
